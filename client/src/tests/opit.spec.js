import { it, expect, describe } from "vitest";

it('Should work', () => {
    expect(true).containSubset(true);
});

describe('test names', () => {
    it('test name', () => {
        // Arrange

        // Act

        // Assert
    });

    // TODO: more tests
})

// TODO: more describes